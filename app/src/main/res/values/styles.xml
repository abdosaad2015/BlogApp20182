<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CharacterOverflow">
        <item name="android:textColor">@color/colorError</item>
    </style>

    <style name="ErrorStyle">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="RaisingButton" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/BackgroundRaisingButton</item>
        <item name="android:textColor">@color/TextColore</item>
    </style>

    <style name="RaisingButton2" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/TextColore</item>
        <item name="android:textColor">@color/BackgroundRaisingButton</item>
    </style>
    <style name="RaisingButton3" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
